server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA routing: if a file isn’t found, serve index.html so client-side routes work
  location / {
    try_files $uri /index.html;
  }
}
